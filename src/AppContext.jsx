/**
 * AppContext.jsx — DEPRECADO
 *
 * Este archivo era un contexto placeholder que nunca llegó a usarse.
 * El estado de la app vive en:
 *   - FinancialContext.jsx → datos de Firebase (auth, tarjetas, transacciones, etc.)
 *   - UIContext.jsx        → estado de UI (vista actual, modo privado, modo glass, fecha)
 *
 * Se mantiene este archivo vacío para no romper ningún posible import accidental.
 * Puede borrarse manualmente cuando quieras.
 */